# Copy to /opt/cloudflared/env on the server and replace with your tunnel token.
# From Cloudflare Zero Trust → Networks → Tunnels → your tunnel → Install / Add connector.
# Point public hostnames at http://traefik:80 (same Docker network).
# chmod 600 /opt/cloudflared/env

TUNNEL_TOKEN=eyJ...