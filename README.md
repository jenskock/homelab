# Homelab

A Git repository for managing Docker Compose stacks in Portainer using GitOps best practices for my homelab.

## Overview

This repository contains Docker Compose stacks designed to be deployed via Portainer's Git Stack feature. Each stack is self-contained with its own configuration, environment variables, and documentation.
