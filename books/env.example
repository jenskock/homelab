# Copy to /opt/books/.env on the server and replace placeholder values.
# chmod 600 /opt/books/.env

# Optional Hardcover metadata provider
# https://docs.hardcover.app/api/getting-started/
HARDCOVER_TOKEN=
