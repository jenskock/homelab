# Books

Calibre-Web-Automated at `books.jenskock.de`.

Drop finished ebooks into the inbox → metadata/convert → library. Inbox files are deleted after processing.

| Host | Purpose |
| --- | --- |
| `/mnt/synology_backup/jens/ebooks/inbox` | Ingest |
| `/mnt/synology_backup/jens/ebooks/books/books` | Library |
| `/opt/books` | Config + `.env` |

Default login: `admin` / `admin123` — change it.
